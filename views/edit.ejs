<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Post</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-900 text-white min-h-screen flex items-center justify-center p-6">
  <div class="w-full max-w-2xl bg-zinc-800 p-8 rounded-lg shadow-md">
    
    <!-- Top Right Logout -->
    <div class="flex justify-end">
      <a class="bg-red-500 hover:bg-red-600 transition px-4 py-2 text-sm rounded-md mb-4" href="/logout">Logout</a>
    </div>

    <!-- Edit Post Section -->
    <h2 class="text-2xl font-semibold mb-4">Edit Your Post</h2>
    <form action="/update/<%= post._id %>" method="post" class="flex flex-col gap-4">
      <textarea
        class="w-full resize-none bg-transparent border border-zinc-700 rounded-md p-4 focus:outline-none focus:ring-2 focus:ring-yellow-500"
        name="content" rows="6" placeholder="What's on your mind?"><%= post.content %></textarea>
      <input
        class="self-start px-5 py-2 text-sm bg-yellow-500 hover:bg-yellow-600 text-black font-semibold rounded-md transition cursor-pointer"
        type="submit" value="Update Post" />
    </form>
  </div>
</body>

</html>
