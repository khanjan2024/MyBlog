<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-900 text-white min-h-screen flex items-center justify-center p-4">
  <div class="w-full max-w-md bg-zinc-800 p-8 rounded-lg shadow-lg">
    <h3 class="text-3xl font-semibold mb-6 text-center">Login to Your Account</h3>
    <form action="/login" method="post" class="space-y-4">
      <input
        class="w-full px-4 py-2 bg-transparent border border-zinc-600 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
        type="email" placeholder="Email" name="email" required />
      <input
        class="w-full px-4 py-2 bg-transparent border border-zinc-600 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
        type="password" placeholder="Password" name="password" required />
      <input
        class="w-full px-4 py-2 bg-blue-600 hover:bg-blue-700 transition-colors rounded-md cursor-pointer text-white font-semibold"
        type="submit" value="Login" />
    </form>
    <div class="text-center mt-6">
      <p class="text-sm text-zinc-400">Don't have an account?</p>
      <a href="/register">
        <button
          class="mt-2 px-5 py-2 bg-green-500 hover:bg-green-600 transition-colors rounded-md font-semibold text-white">
          Create Account
        </button>
      </a>
    </div>
  </div>
</body>

</html>

</html>